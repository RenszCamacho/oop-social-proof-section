class Buyer {
  constructor(image, fullName, verified, review) {
    this.image = image;
    this.fullName = fullName;
    this.verified = verified;
    this.review = review;
  }
}

export default Buyer;
